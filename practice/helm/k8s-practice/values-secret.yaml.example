# Copy this file to values-secret.yaml and fill in your values
# DO NOT commit values-secret.yaml to git
# Usage: helm install myapp ./helm/k8s-practice -f ./helm/k8s-practice/values-secret.yaml

secrets:
  postgres:
    database: myapp
    username: myuser
    password: <your-secure-password>
  grafana:
    adminUser: admin
    adminPassword: <your-secure-password>
